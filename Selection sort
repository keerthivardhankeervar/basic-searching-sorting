#Selection Sort
START 
  READ array A of size N 
  FOR i = 0 TO N-2 
    MIN_INDEX = i 
    FOR j = i+1 TO N-1 
      IF A[j] < A[MIN_INDEX] THEN 
        MIN_INDEX = j 
      END IF 
    END FOR 
    SWAP A[i] and A[MIN_INDEX] 
  END FOR 
PRINT sorted array A 
END
